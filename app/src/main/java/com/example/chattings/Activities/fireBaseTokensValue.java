package com.example.chattings.Activities;

import com.google.firebase.messaging.FirebaseMessagingService;

public class fireBaseTokensValue extends FirebaseMessagingService {

/*

    @Override
    public void onTokenRefress(@NonNull String token){
        //Get Updated InstanceId Token
    String RefreshedToken = FirebaseInstallations.getInstance().getToken());
        Log.d(TAG,"Refreshed Token: " + token);


        // TODO: Implement this method to send any registration to your app's servers.
        // If you want to send messages to this application instance or
        // manage this apps subscriptions on the server side, send the
        // FCM registration token to your app server.
        sendRegistrationToServer(token);
    }


 */

}
